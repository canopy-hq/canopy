---
phase: quick
plan: 260401-w64
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "TabBar appears only above the terminal/pane area, not above the sidebar"
    - "Sidebar spans full height from title bar to status bar"
  artifacts:
    - path: "src/App.tsx"
      provides: "Updated layout with TabBar inside content column"
  key_links:
    - from: "src/App.tsx"
      to: "TabBar"
      via: "rendered inside content flex column, not above sidebar row"
---

<objective>
Move TabBar from above the sidebar+content row into the content column so it sits only above the pane area. Sidebar gains full height.

Purpose: Visual separation between navigation (sidebar) and workspace tabs (content column).
Output: Updated App.tsx layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure App.tsx layout</name>
  <files>src/App.tsx</files>
  <action>
In the JSX return block, move `<TabBar />` from its current position (above the flex-row div) into the content column div, above the tab pane map. The content column div should become a flex column:

Before (lines 114-134):
```tsx
<div className="h-screen w-screen overflow-hidden flex flex-col bg-bg-primary">
  <TabBar />
  <div className="flex-1 min-h-0 flex flex-row">
    <Sidebar />
    <div className="flex-1 min-w-0 relative">
      {tabs.map(...)}
    </div>
  </div>
  <StatusBar />
  <ErrorToastRegion />
</div>
```

After:
```tsx
<div className="h-screen w-screen overflow-hidden flex flex-col bg-bg-primary">
  <div className="flex-1 min-h-0 flex flex-row">
    <Sidebar />
    <div className="flex-1 min-w-0 flex flex-col">
      <TabBar />
      <div className="flex-1 min-h-0 relative">
        {tabs.map(...)}
      </div>
    </div>
  </div>
  <StatusBar />
  <ErrorToastRegion />
</div>
```

Key changes:
- Remove `<TabBar />` from top-level flex-col
- Inner content div: add `flex flex-col` (was just `flex-1 min-w-0 relative`)
- Add `<TabBar />` as first child of the content column
- Pane map wrapper: remove `relative` (moved up), add `flex-1 min-h-0 relative` to take remaining height
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>TabBar renders to the right of sidebar, sidebar spans full height, no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Moved TabBar inside content column — sidebar now full-height, tabs only above pane area</what-built>
  <how-to-verify>
    1. Run `bun tauri dev`
    2. Confirm sidebar spans full height (top to status bar)
    3. Confirm TabBar appears only above the terminal pane, not above the sidebar
    4. Confirm adding/switching tabs still works (Cmd+T, Cmd+1..9)
    5. Confirm split panes still render correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
bun run typecheck
</verification>

<success_criteria>
TabBar is visually scoped to the content column. Sidebar spans full height. No regressions in tab switching or pane layout.
</success_criteria>

<output>
No SUMMARY file needed for quick tasks.
</output>
