---
phase: 04-git-integration-sidebar
plan: 04
type: execute
wave: 1
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/stores/tabs-store.ts
  - src/stores/workspace-store.ts
  - src/components/WorkspaceTree.tsx
  - src/stores/__tests__/tabs-store.test.ts
  - src/stores/__tests__/workspace-store.test.ts
  - src/components/__tests__/WorkspaceTree.test.tsx
autonomous: true
gap_closure: true
requirements: ["SIDE-01", "SIDE-02"]

must_haves:
  truths:
    - "Selecting a branch/worktree in sidebar switches the active tab to one associated with that workspace item"
    - "Each workspace item gets its own tab with independent pane layout"
    - "Returning to a previously selected workspace item restores its tab and terminal state"
    - "New workspace item selection creates a tab with one default terminal pane"
    - "Sidebar tree items show pointer cursor on hover"
  artifacts:
    - path: "src/stores/tabs-store.ts"
      provides: "findOrCreateTabForWorkspaceItem and workspaceItemId field on Tab"
      contains: "workspaceItemId"
    - path: "src/stores/workspace-store.ts"
      provides: "selectWorkspaceItem action that triggers tab switch"
      contains: "selectWorkspaceItem"
    - path: "src/components/WorkspaceTree.tsx"
      provides: "Sidebar selection wired to workspace-tab switching + pointer cursor"
      contains: "cursor-pointer"
  key_links:
    - from: "src/components/WorkspaceTree.tsx"
      to: "src/stores/workspace-store.ts"
      via: "selectWorkspaceItem action on tree item click"
      pattern: "selectWorkspaceItem"
    - from: "src/stores/workspace-store.ts"
      to: "src/stores/tabs-store.ts"
      via: "selectWorkspaceItem calls findOrCreateTabForWorkspaceItem"
      pattern: "findOrCreateTabForWorkspaceItem"
---

<objective>
Close GAP-1 (workspace-terminal association) and GAP-2 (pointer cursor) from Phase 04 UAT.

Selecting a branch/worktree in the sidebar must switch the right panel to show that workspace item's terminals. Each workspace item gets its own tab. Returning to a previously visited item restores its tab. Also fix text cursor on tree items.

Purpose: Without this, sidebar selection is decorative — the core UX of workspace-driven terminal management is broken.
Output: Working sidebar-to-tab switching with per-workspace-item terminal isolation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-git-integration-sidebar/04-02-SUMMARY.md
@.planning/phases/04-git-integration-sidebar/04-HUMAN-UAT.md

<interfaces>
<!-- Current Tab interface from tabs-store.ts -->
```typescript
export interface Tab {
  id: string;
  label: string;
  paneRoot: PaneNode;
  focusedPaneId: PaneId | null;
}
```

<!-- Current workspace-store interfaces -->
```typescript
export interface Workspace {
  id: string;
  path: string;
  name: string;
  branches: BranchInfo[];
  worktrees: WorktreeInfo[];
  expanded: boolean;
}

interface WorkspaceState {
  workspaces: Workspace[];
  sidebarVisible: boolean;
  sidebarWidth: number;
  selectedItemId: string | null;
  // ... actions
}
```

<!-- WorkspaceTree selection handler -->
```typescript
function handleSelectionChange(keys: Selection) {
  if (keys === 'all') return;
  const selected = [...keys][0];
  setSelectedItem(selected ? String(selected) : null);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workspace-item-to-tab association in tabs-store and workspace-store</name>
  <files>
    src/stores/tabs-store.ts,
    src/stores/workspace-store.ts,
    src/stores/__tests__/tabs-store.test.ts,
    src/stores/__tests__/workspace-store.test.ts
  </files>
  <read_first>
    src/stores/tabs-store.ts,
    src/stores/workspace-store.ts,
    src/stores/__tests__/tabs-store.test.ts,
    src/stores/__tests__/workspace-store.test.ts
  </read_first>
  <behavior>
    - Test: Tab interface has optional workspaceItemId field (default undefined for generic tabs)
    - Test: findOrCreateTabForWorkspaceItem with new itemId creates a new tab with that workspaceItemId and label from item name, switches to it
    - Test: findOrCreateTabForWorkspaceItem with existing itemId switches to the existing tab (no new tab created)
    - Test: findOrCreateTabForWorkspaceItem sets the new tab's label to the branch/worktree name
    - Test: workspace-store selectWorkspaceItem(itemId, itemLabel) sets selectedItemId AND calls tabs-store findOrCreateTabForWorkspaceItem
    - Test: selectWorkspaceItem(null) only clears selectedItemId, does not change tabs
    - Test: existing tabs-store tests still pass (workspaceItemId is optional, no breaking changes)
  </behavior>
  <action>
    **tabs-store.ts changes:**

    1. Add `workspaceItemId?: string` to the `Tab` interface (optional — existing generic tabs have undefined).

    2. Add `findOrCreateTabForWorkspaceItem(itemId: string, label: string): void` action to `TabsState`:
       - Search `state.tabs` for a tab where `tab.workspaceItemId === itemId`
       - If found: set `state.activeTabId = tab.id` (switch to it)
       - If not found: create new tab via same pattern as `makeTab()` but with `workspaceItemId: itemId` and `label: label`, push to `state.tabs`, set as `activeTabId`

    3. Update `makeTab()` to accept optional params: `function makeTab(opts?: { workspaceItemId?: string; label?: string }): Tab` — uses `opts?.label ?? 'Terminal'` for label, `opts?.workspaceItemId` for the field.

    **workspace-store.ts changes:**

    4. Add `selectWorkspaceItem(itemId: string | null, itemLabel?: string): void` action to `WorkspaceState`:
       - Sets `state.selectedItemId = itemId`
       - If `itemId` is not null and `itemLabel` is provided: call `useTabsStore.getState().findOrCreateTabForWorkspaceItem(itemId, itemLabel)` (cross-store call, same pattern as other Zustand cross-store calls)
       - If `itemId` is null: only clear selectedItemId, do nothing with tabs

    5. Keep existing `setSelectedItem` action for backward compat but have `selectWorkspaceItem` be the primary action used by the tree.

    **Test updates:**

    6. In `tabs-store.test.ts`, add a `describe('findOrCreateTabForWorkspaceItem')` block:
       - `it('creates new tab with workspaceItemId when no match exists')` — call with ('ws1-branch-main', 'main'), verify new tab has workspaceItemId='ws1-branch-main', label='main', is activeTabId
       - `it('switches to existing tab when match exists')` — call twice with same id, verify tabs.length is 2 (initial + 1 workspace tab), activeTabId matches the workspace tab
       - `it('new workspace tab has sentinel leaf pane')` — verify paneRoot.type === 'leaf' and ptyId === -1

    7. In `workspace-store.test.ts`, add a `describe('selectWorkspaceItem')` block:
       - `it('sets selectedItemId and creates workspace tab')` — mock or spy on tabs-store, call selectWorkspaceItem('item1', 'main'), verify selectedItemId='item1'
       - `it('selectWorkspaceItem(null) clears selection without tab change')` — verify selectedItemId becomes null
  </action>
  <verify>
    <automated>cd /Users/pierre/Workspace/perso/superagent && npx vitest run src/stores/__tests__/tabs-store.test.ts src/stores/__tests__/workspace-store.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'workspaceItemId' src/stores/tabs-store.ts returns 0
    - grep -q 'findOrCreateTabForWorkspaceItem' src/stores/tabs-store.ts returns 0
    - grep -q 'selectWorkspaceItem' src/stores/workspace-store.ts returns 0
    - grep -q 'findOrCreateTabForWorkspaceItem' src/stores/__tests__/tabs-store.test.ts returns 0
    - All tests in tabs-store.test.ts and workspace-store.test.ts pass
    - No existing test is broken
  </acceptance_criteria>
  <done>
    Tab interface has workspaceItemId field. findOrCreateTabForWorkspaceItem creates or switches to workspace-associated tabs. selectWorkspaceItem in workspace-store bridges sidebar selection to tab switching. All existing + new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire WorkspaceTree selection to tab switching + fix pointer cursor</name>
  <files>
    src/components/WorkspaceTree.tsx,
    src/components/__tests__/WorkspaceTree.test.tsx
  </files>
  <read_first>
    src/components/WorkspaceTree.tsx,
    src/components/__tests__/WorkspaceTree.test.tsx,
    src/stores/workspace-store.ts
  </read_first>
  <action>
    **WorkspaceTree.tsx changes:**

    1. Replace usage of `setSelectedItem` with `selectWorkspaceItem` from workspace-store:
       ```typescript
       const selectWorkspaceItem = useWorkspaceStore((s) => s.selectWorkspaceItem);
       ```
       Remove the `setSelectedItem` selector.

    2. Update `handleSelectionChange` to pass the item label:
       ```typescript
       function handleSelectionChange(keys: Selection) {
         if (keys === 'all') return;
         const selected = [...keys][0];
         if (!selected) {
           selectWorkspaceItem(null);
           return;
         }
         const selectedStr = String(selected);
         // Find the label for this item
         const label = findItemLabel(selectedStr);
         selectWorkspaceItem(selectedStr, label);
       }
       ```

    3. Add helper `findItemLabel(itemId: string): string` that searches workspaces to find the branch/worktree name matching the item ID pattern:
       - If itemId matches `{wsId}-branch-{name}` pattern: return the branch name
       - If itemId matches `{wsId}-wt-{name}` pattern: return the worktree name
       - If itemId matches a workspace id directly: return the workspace name
       - Fallback: return 'Terminal'

       Implementation: iterate `workspaces`, for each ws check:
       - `itemId === ws.id` → return ws.name
       - `itemId.startsWith(ws.id + '-branch-')` → extract name after prefix, return it
       - `itemId.startsWith(ws.id + '-wt-')` → extract name after prefix, return it

    4. **Fix GAP-2 (pointer cursor):** Add `cursor-pointer` to the className of ALL TreeItem elements for branches, worktrees, and repo headers. Specifically update the className callback on lines ~130, ~142, ~152 from:
       ```
       `outline-none ${isSelected ? '...' : 'hover:bg-bg-tertiary'}`
       ```
       to:
       ```
       `outline-none cursor-pointer ${isSelected ? '...' : 'hover:bg-bg-tertiary'}`
       ```

    **WorkspaceTree.test.tsx changes:**

    5. Update test mocks to include `selectWorkspaceItem` instead of (or in addition to) `setSelectedItem`. Ensure existing tests still pass by verifying the mock is called correctly.

    6. Add test: `it('shows pointer cursor on tree items')` — render WorkspaceTree with a workspace, query a TreeItem, verify its className contains 'cursor-pointer'.
  </action>
  <verify>
    <automated>cd /Users/pierre/Workspace/perso/superagent && npx vitest run src/components/__tests__/WorkspaceTree.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'selectWorkspaceItem' src/components/WorkspaceTree.tsx returns 0
    - grep -q 'cursor-pointer' src/components/WorkspaceTree.tsx returns 0
    - grep -c 'cursor-pointer' src/components/WorkspaceTree.tsx returns at least 3 (one per TreeItem type: repo, branch, worktree)
    - grep -q 'setSelectedItem' src/components/WorkspaceTree.tsx returns 1 (removed) — actually verify it is NOT present: ! grep -q 'setSelectedItem' src/components/WorkspaceTree.tsx
    - All WorkspaceTree tests pass
  </acceptance_criteria>
  <done>
    Selecting a branch/worktree in sidebar triggers selectWorkspaceItem which finds-or-creates a tab for that item. Tree items show pointer cursor. GAP-1 and GAP-2 both resolved.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/pierre/Workspace/perso/superagent && npx vitest run --reporter=verbose 2>&1 | tail -20` — all 107+ tests pass (existing + new)
2. `grep -q 'workspaceItemId' src/stores/tabs-store.ts` — workspace-tab association exists
3. `grep -q 'selectWorkspaceItem' src/stores/workspace-store.ts` — bridge action exists
4. `grep -q 'cursor-pointer' src/components/WorkspaceTree.tsx` — pointer cursor applied
5. `grep -q 'findOrCreateTabForWorkspaceItem' src/stores/tabs-store.ts` — tab lookup/create exists
</verification>

<success_criteria>
- Selecting a sidebar branch/worktree creates or switches to a dedicated tab for that item
- Returning to a previously selected item restores its tab (no duplicate tab created)
- New workspace items get a fresh tab with sentinel leaf pane (terminal spawns on mount)
- All tree items show pointer cursor on hover
- All existing tests pass, new tests cover the workspace-tab association
- GAP-1 (workspace-terminal association) and GAP-2 (pointer cursor) are both closed
</success_criteria>

<output>
After completion, create `.planning/phases/04-git-integration-sidebar/04-04-SUMMARY.md`
</output>
